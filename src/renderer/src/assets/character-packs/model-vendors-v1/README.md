# Model Vendors Character Pack v1

Reusable Agent Arena character assets generated as independent images.

Each character includes one full portrait and three emotion avatars: neutral, thinking, confident.

These assets are intentionally stored outside the current live character registry so they can be wired into character editing and in-game emotion rendering later without changing existing saved data.
